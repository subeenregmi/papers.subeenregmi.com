:8080 {
	root * /srv
	encode gzip

	# Quartz emits page.html but links to /page, so try the .html form too.
	try_files {path} {path}.html {path}/index.html
	file_server

	handle_errors {
		rewrite * /404.html
		file_server
	}
}
